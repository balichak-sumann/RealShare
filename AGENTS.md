# Agent Rules

## Git Workflows
- **NEVER** push code to remote repositories (e.g., `git push`) unless the user explicitly asks you to do so. Wait for their explicit permission or command before pushing any code.
